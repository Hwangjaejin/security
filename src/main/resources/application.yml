server:
  port: 8090
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

# ??? ??? ??? ?????? ??? ??? ????. (?? ??? ? ??? ??? ??)
spring:
  profiles:
    active: local
  datasource:
    #    url: jdbc:h2:mem:test # jdbc:h2:mem:test => ??? DB ??. h2 database ???? ???.
    url: jdbc:h2:tcp://localhost/~/security
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create       # create : ?? ??? ?? entity? ? drop? ?? create?? application? ????.
    properties:              # create-drop : create? ?? ??? application ????? drop query? ??.
      hibernate:
        #        show_sql: true      # SQL ??? console? ??
        format_sql: true     # ??? ????? ???? ??
        use_sql_comments: true
  data:
    web:
      pageable:
        default-page-size: 10 # default : 20
        max-page-size: 2000
logging:
  level:
    org.hibernate.SQL: debug # SQL ??? ??? ??
#    org.hibernate.type : trace  # ????? ????? ? ? ??